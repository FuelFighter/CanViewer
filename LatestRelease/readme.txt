How to use:
1) Program an UM with one of the .hex files. 
   If the UM you are using has an AT90CAN32, 
   use the *32.hex file, if it has an 
   AT90CAN128, use the *128.hex file.
2) Open CanViewer_vX_Y.exe
